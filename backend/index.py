from app.main import app

# This file sits at the root of the backend folder.
# Vercel will use this as the entrypoint for the serverless function.
# Since it is at the root, it automatically finds the `app` folder and `requirements.txt`.
